# VULN: weak, hardcoded-style secret checked into example config (CWE-798)
# A real app must load this from a secrets manager / vault, never a committed file.
JWT_SECRET=supersecret123

MONGO_URI=mongodb://localhost:27017/vulnerable_mern
PORT=5000
